{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "npm run build",
			"group": {
				"kind": "build",
				"isDefault": true
			},
            "type": "process",
			"command": "npm",
			"args": ["run", "build"],
			"problemMatcher": ["$tsc"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
		},
	]
}